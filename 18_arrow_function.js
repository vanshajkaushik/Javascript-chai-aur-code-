// its is just a new method to make a function

const chai = () => {
    console.log("hello");
    
}

chai()

// Implicit return arrow function

const addtwo = (num1,num2) => (num1+num2) // if we use ( ) no need to return but if we use { } we need to write return 

console.log(addtwo(5,6))

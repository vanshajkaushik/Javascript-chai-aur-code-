//arrays in js are resizable and can be declared as 

var myarr = [1,2,3,4,5]

console.log(myarr[2]);


//they can store single type or multiple type data

var newarr = [1 ,2,"vanshaj"]

console.log(newarr[2]);

// similar to strings and dates there are different methods in arrays which can be seen in prototype 
// by going in console 